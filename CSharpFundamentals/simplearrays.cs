using System;

namespace Samples
{
	public class SimpleArrays
	{
        //Uncomment the following while executing this code.--------
        /*
		public static void Main()
		{
			// Declare the array of two Dimensions:
      			int[,] arr = new int[2,4];

      			// Initialize the elements:
      			arr[0,0] = 10;
      			arr[0,1] = 20;
			arr[0,2] = 30;
			arr[0,3] = 40;				
			arr[1,0] = 11;
			arr[1,1] = 22;
			arr[1,2] = 33;
			arr[1,3] = 44;

			// Display the array elements:
	      		for (int i=0; i < arr.GetLength(0); i++) 
      			{
         			//Console.Write("Element({0}): ", i);

         			for (int j = 0 ; j < arr[i].GetLength(1) ; j++)
            				Console.Write("arr[{0},{1}]={3}\t",i,j arr[i][j]);

         			Console.WriteLine();
      			}
		}
         * */
	}
}